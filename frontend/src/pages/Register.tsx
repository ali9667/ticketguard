import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import type {FormEvent} from 'react';import {api} from '../lib/api';import {useAuth} from '../lib/auth';import {apiErrorMessage} from '../lib/types';
export function Register(){const{login}=useAuth();const[form,setForm]=useState({firstName:'',lastName:'',email:'',password:''});const[error,setError]=useState('');const nav=useNavigate();const submit=async(e:FormEvent)=>{e.preventDefault();setError('');try{await api.post('/auth/register',form);await login(form.email,form.password);nav('/tickets')}catch(x){setError(apiErrorMessage(x,'Unable to create account.'))}};return <div className="page"><div className="hero" style={{minHeight:'auto',gridTemplateColumns:'1fr 1fr'}}><div><span className="eyebrow">Join TicketGuard</span><h1>Own the ticket, not just the barcode.</h1><p>Your account is the anchor for ticket ownership, transfers and verified entry.</p></div><div className="card"><h2>Create account</h2><form onSubmit={submit} className="form-grid"><div className="field"><label>First name</label><input className="input" value={form.firstName} onChange={e=>setForm({...form,firstName:e.target.value})} required/></div><div className="field"><label>Last name</label><input className="input" value={form.lastName} onChange={e=>setForm({...form,lastName:e.target.value})} required/></div><div className="field full"><label>Email</label><input className="input" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></div><div className="field full"><label>Password</label><input className="input" type="password" minLength={12} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></div><button className="btn primary">Create account</button></form>{error&&<div className="notice error" style={{marginTop:15}}>{error}</div>}<p className="muted">Already registered? <Link to="/login">Sign in</Link></p></div></div></div>}
