import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import type {FormEvent} from 'react';import {useAuth} from '../lib/auth';import {apiErrorMessage} from '../lib/types';
export function Login(){const{login}=useAuth();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const nav=useNavigate();const submit=async(e:FormEvent)=>{e.preventDefault();setError('');try{await login(email,password);nav('/tickets')}catch(x){setError(apiErrorMessage(x,'Unable to sign in.'))}};return <div className="page"><div className="hero" style={{minHeight:'auto',gridTemplateColumns:'1fr 1fr'}}><div><span className="eyebrow">Secure access</span><h1>Carry your ticket. Keep your ownership.</h1><p>Sign in to access verified tickets, secure transfers and your digital entry pass.</p></div><div className="card"><h2>Welcome back</h2><form onSubmit={submit} className="field"><label>Email<input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="btn primary" type="submit">Sign in</button>{error&&<div className="notice error">{error}</div>}</form><p className="muted">New to TicketGuard? <Link to="/register">Create an account</Link></p></div></div></div>}
